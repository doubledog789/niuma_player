# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Internal planning / spec docs (kept local only, not for public repo)
/doc/plans/
/.superpowers/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/

# Vendored ijkplayer AAR — pulled by android/scripts/compile/download-prebuilt.sh.
# Keep .pom (version record) in git, keep .aar out (13 MB binary).
android/libs/
android/localmaven/**/*.aar
AGENTS.md
.superpowers/

# Claude Code scheduled tasks lock (local)
.claude/

# Xcode 默认 AppIcon placeholder（Icon-App-*x*@*x.png）— 例子用的是简化命名
# Icon-*.png 那套（被 Contents.json 引用），Icon-App-* 是 flutter create
# 残留死文件，不入库
example/ios/Runner/Assets.xcassets/AppIcon.appiconset/Icon-App-*.png
